export const moduleName = '@palladium-ethiopia/esm-order-catalog-ethio-app';

/** Matches `order_catalog_hierarchy_concepts.csv` — All Orderables root. */
export const defaultAllOrderablesConceptUuid = 'a1b2c3d4-e5f6-4789-a012-3456789abcde';

/** Workspace v2 name — must match `routes.json` workspaces2 entry. */
export const ethioOrderCatalogWorkspaceName = 'ethio-order-catalog';

/** Workspace window name — must match `routes.json` workspaceWindows2 entry. */
export const ethioOrderCatalogWindowName = 'ethio-order-catalog-window';
